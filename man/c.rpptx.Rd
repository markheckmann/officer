% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pptx_concat.R
\name{c.rpptx}
\alias{c.rpptx}
\title{Concatenate rpptx objects}
\usage{
\method{c}{rpptx}(...)
}
\arguments{
\item{...}{\code{rpptx} objects to concatenate.}
}
\value{
A new \code{rpptx} object with slides from all inputs. The input objects
are not modified.
}
\description{
S3 method that allows using \code{c()} to concatenate \code{rpptx} objects.
The first argument is used as the target (its template and properties are kept),
and all subsequent presentations' slides are appended.
}
\examples{
pptx1 <- read_pptx()
pptx1 <- add_slide(pptx1, layout = "Title and Content")

pptx2 <- read_pptx()
pptx2 <- add_slide(pptx2, layout = "Title and Content")

result <- c(pptx1, pptx2)
length(result) # 2
}
\seealso{
\code{\link[=pptx_concat]{pptx_concat()}}
}
